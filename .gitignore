# Secrets
.env

# Local config with personal details (copy config.example.yaml to config.yaml)
config.yaml

# Logs (runtime output)
logs/*.csv

# Local assets (resume, cover letter, etc.)
assets/

# Personal recipient lists (keep only the sample in the repo)
my_recipients.csv
recipients.csv
recipients.local.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
